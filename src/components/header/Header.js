import React from "react";
import ReactDOM from "react-dom";
import App from "../app/App";

function Header(){
    return(
        <div className="header">
            <h1 className="header-title">Bot universe</h1>
        </div>
    );
}

export default Header;